<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <android.support.v7.widget.CardView
        android:id="@+id/slide_container"
        android:layout_width="@dimen/screenplay_editor_card_width"
        android:layout_height="@dimen/screenplay_editor_card_height"
        card_view:cardUseCompatPadding="true"
        card_view:cardCornerRadius="4dp"
        card_view:cardElevation="4dp">

    <TextView
        android:id="@+id/slide_only_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:text="Large Text"
        android:maxLength="10"
        android:ellipsize="end"
        android:gravity="center"
        android:textSize="@dimen/slide_only_text_size"/>

    <ImageView
        android:id="@+id/img_trash"
        android:layout_margin="@dimen/margin_small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/trash"
        android:layout_gravity="right|top" />
    </android.support.v7.widget.CardView>

</LinearLayout>